# Результаты проверки

Проверено 8 сентября 2026 года на macOS, OpenJDK 21.0.12, PostgreSQL 18, Maven 3.9.9.

Итоговая команда: Maven Wrapper `clean package` с `TEST_DB_URL`, указывающим на отдельную временную PostgreSQL-базу. Сборка прошла успешно: 12 тестов, 0 ошибок, 0 падений, 0 пропусков. JAR: `target/java-hospital-1.0.0.jar`.

| Проверка | Результат |
|---|---|
| Соль пароля, проверка правильного/неправильного пароля | ✅ |
| Регистрация, нормализация и уникальность логина, валидация | ✅ |
| Недоступность чужих записей и функций администратора пациенту | ✅ |
| Прошлое, выходные, неверное время, горизонт записи, отсутствующий врач | ✅ |
| Конфликт времени у врача и пациента, освобождение после отмены | ✅ |
| Неудачный перенос сохраняет исходные данные; успешный увеличивает счётчик | ✅ |
| Границы начала/окончания приёма, запрещённые переходы и удаление | ✅ |
| Поиск, фильтры с включёнными границами, сортировка, статистика | ✅ |
| CRUD пользователей, запрет удаления администратора и связанных пользователей | ✅ |
| Чтение сохранённых записей новым экземпляром репозитория | ✅ |
| CSV: BOM, кавычки, формулы; XLSX: повторное чтение Apache POI, даты; запрет перезаписи | ✅ |
| Два параллельных бронирования одного врача: ровно один успех | ✅ |

Дополнительно проверен готовый JAR через stdin/stdout на отдельной демонстрационной базе: вход и регистрация, права, бронирование, перенос, отмена, удаление, статистика, поиск, фильтры, сортировка, таблицы, документация, оба экспорта, текст вместо числа, неверные пароль и дата, EOF, повторная инициализация и недоступная БД. Лог: `cli-verification.txt`.

`demo-appointments.xlsx` и `demo-appointments.csv` получены через пункт экспорта CLI администратора из PostgreSQL: 12 записей. В XLSX дополнительно проверены структура ZIP/XML и наличие 13 строк с заголовком.

Сборка проверена через поставляемый Maven Wrapper. SQL-скрипт для сдачи и встроенный ресурс совпадают. Docker Compose и запуск на Windows отдельно не проверялись; проверка БД выполнялась на локальном PostgreSQL 18. Временная тестовая PostgreSQL после проверки остановлена.
